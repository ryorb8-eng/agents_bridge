## [2026-07-17 11:30] Bridge Chrome DebugProfile Win11↔Linux — lock-down, launcher bug, tunnel persistence

- **SSH lock-down (DONE):** `C:\Users\ryoro\.ssh\authorized_keys` sekarang single scoped line = `command="powershell.exe -NoProfile -ExecutionPolicy Bypass -File C:\bridge\chrome-debug-gate.ps1",restrict,from="192.168.1.3" <IKLmv key>`. Linux agent HANYA bisa panggil gatekeeper (status|kill|launch:<Profile>|tunnel|scan). `whoami` → REJECTED. Verified via `ssh win11 status` / `launch:Profile 14` / `tunnel`.
- **Gatekeeper aksi:** `chrome-debug-gate.ps1` di Win11 punya action `tunnel` yang delegasi ke `bridge-tunnel.ps1` (buka reverse forward sebagai scheduled task). Satu key cukup — TIDAK perlu second key (win11tunnel dihapus).
- **Launcher profile-split bug (DONE + deployed):** `Start-Process -ArgumentList @("--profile-directory=$profile")` dengan `$profile="Profile 14"` memecah jadi dua arg (`--profile-directory=Profile` + `14`) → Chrome buka dir "Profile" anonim (no account). FIX: pass full arg string as ONE element — `$args = "... --profile-directory=`"$profile`""`; `Start-Process -ArgumentList $args`. Verified via raw argv: `--profile-directory="Profile 14"`. Sama untuk `--user-data-dir`.
- **Tunnel persistence (DONE):** reverse forward `-R <port>:127.0.0.1:<port> linux` HARUS jalan sebagai SCHEDULED TASK (`BridgeChromeTunnel`), bukan child langsung dari session SSH — child ssh -R di-kill begitu `ssh win11` return (listener GONE t=1s). Task `BridgeChromeDebug` (Chrome launch) pakai pola sama.
- **`-R` target pakai `127.0.0.1` (DONE):** Win11 resolve `localhost`→`::1` duluan, tapi Chrome bind IPv4 `127.0.0.1` → forward `localhost` kasih empty reply. Pin `127.0.0.1`.
- **VS Code squat (handled):** VS Code + Insiders (`ms-vscode-remote.remote-ssh`) auto-forward & squat port (18080, 18181, 18322, 20128–20130, 57518). Gatekeeper auto-relocate Chrome ke port bebas di 18080–19980, persist ke `C:\bridge\debug_port.txt` (launcher + gatekeeper + tunnel baca file ini). Status saat ini: Chrome di 18180.
- **`bridge_cdp_tunnel.sh`:** drive semua aksi via `ssh win11 <action>` (forced-command), fallback ke `-File` kalau key plain. Self-heal reap zombie listener (sudo) + arg `launch:"Profile 14"` di-quote.
- **GPT logging SELESAI di-robustkan (DONE):** root cause kenapa `web-bridge-chatgpt.log` hilang = capture-failure `process.exit(1)` di `readLastReply` jalan SEBELUM `logConversation` (di success path). FIX: (1) IIFE wrap try/catch/finally → `logConversation` SELALU jalan (sukses MAUPUN gagal); (2) `readLastReply` sekarang THROW (bukan `process.exit`) supaya `finally` tetap tereksekusi; (3) `logConversation` terima `profile` + `error` → ditulis ke entry JSON; `page` boleh `undefined`; (4) akhir run: `leaveOpen = KEEP_OPEN || !!runError` → bila error, browser TETAP TERBUKA (inspeksi DOM) + exit 1 + log tercatat. File temp `gpt/__inspect.ts` DIHAPUS. `tsc --noEmit` PASS.
- **GPT capture FIX TERPASANG (DONE 2026-07-17):** solusi krusial langsung disimpan (tidak ditunda). Metode terbaik = **`innerText` node `[data-message-author-role="assistant"]` terakhir = AUTHORITATIVE** (6528 char jawaban asli, paling mudah+akurat), **TOLAK clipboard** (terbukti KOTOR: 9023 char `match=false`). Tombol `Salin respons` = konfirmasi, bukan sumber teks. Terapkan ke: (1) `web-dom-general §4` scrape order diubah (innerText=1, tombol=2, turndown=3, Ctrl+A/C=4); (2) `web-dom-general §7.2` aturan "WAJIB simpan solusi krusial langsung ke skill, dilarang frasa 'perbaikan nanti'"; (3) `web-dom-chatgpt §3` hapus banner drift SALAH (`.markdown` masih match 3 node; failure lalu murni page salah) + tulis rule authoritative + page-guard; (4) `readLastReply` di `gpt/bridge-cdp-gpt_new.ts` + `_continue.ts` rewrite: page-guard (`/chatgpt\.com/` else `goto CHAT_URL`) + `ASSISTANT_CONTAINER='[data-message-author-role="assistant"]'` + innerText authoritative + throw (bukan exit). Verified LIVE: `tsx gpt/bridge-cdp-gpt_continue.ts` capture 6528-char, exit 0, tab user tidak ditutup. `tsc --noEmit` PASS.
- **GPT copy-button LOCALE-AWARE (DONE 2026-07-17):** `aria-label` tombol copy ikut bahasa UI. id=`"Salin respons"`, en=`"Copy response"` (markup sama persis, cuma label beda). `data-testid="copy-turn-action-button"` **SAMA di semua bahasa** → pakai `data-testid` sbg anchor utama tombol copy, BUKAN teks `aria-label` (rawan miss kalau locale beda). Ditulis di `web-dom-chatgpt §3`. (Inget: tombol copy tetap BUKAN sumber teks — innerText node assistant terakhir yg authoritative.)
- **GPT STOP-button detection (DONE 2026-07-17):** deteksi "GPT masih menjawab" = cek `button[data-testid="stop-button"]` ADA → masih generate, JANGAN capture. LOCALE-AWARE: id=`"Hentikan jawaban"`, en=`"Stop answering"` (id=`composer-submit-button`, `data-testid="stop-button"`). Anchor stabil = `data-testid` (bukan aria-label). Tulis `web-dom-chatgpt §3.1`.
- **copy-button BUKAN deteksi "masih menjawab" (KOREKSI MASTER 2026-07-17):** jawaban SEBELUMNYA pun punya copy-button → bukan indikator generate. copy-button HANYA metode capture/fallback (§2/§4), bukan deteksi. Terapkan ke `web-dom-chatgpt §3.1` + `web-dom-general §3` (ganti indikator copy-button → stop-button sbg PRIMARY; copy-button dihapus dari deteksi).
- **Z STOP-button detection (DONE 2026-07-17):** `web-dom-z §3.1` — deteksi "Z masih menjawab" = cek `[aria-label="Stop"]` ADA (tombol terluar `div.flex[aria-label="Stop"]`, BUKAN button dalam yg tanpa aria-label) → masih generate, JANGAN capture. Anchor = `[aria-label="Stop"]`. Hapus juga rujukan copy-button sbg sinyal selesai di `web-dom-z §3` (copy-button = capture only). Konsisten dgn koreksi MASTER.
- **list_profil_vendor.md + wiring profil (DONE 2026-07-17):** file BARU `docs/bridge/list_profil_vendor.md` = pemetaan profil↔vendor (umum §1: Profile 14=default semua vendor, Profile 2=cadangan rate-limit; spesifik §2: URL continue login-specific→profil; log §3). AGENTS.md `PHASE 0.5` = agents/subagents WAJIB cek file ini + `.log` wajib field `profile`. Wiring kode: (a) `_new.ts` gpt/claude/z `logConversation` + entry JSON dapat field `profile` (gpt sudah; claude/z ditambah); `PROFILE = BRIDGE_PROFILE || 'Profile 14'` (gpt_new default ''→'Profile 14'). (b) `_continue.ts` gpt/claude/z: `PROFILE` const + comment "URL login-specific → hanya Profile 14" + console `[bridge] Profil: ...`. (c) `_continue.ts` BELUM tulis `.log` → catat TODO di §4 list_profil (MASTER belum minta tambah log ke continue). `tsc --noEmit` PASS.
- **Global score efektifitas metode + naik-kelas (DONE 2026-07-17):** `web-dom-general §7.3` — tiap `web-dom-<remote>` pelihara skor per-metode capture; metode lebih sering sukses NAIK KELAS (pindah prioritas §4). Skor dari run terukur (replyChars>0=sukses), bukan tebakan; reset bila DOM drift. `web-dom-chatgpt §7` = tabel skor aktual (innerText #1, copy #2 konfirmasi, turndown #3, Ctrl+A/C #4). Berlaku semua vendor.
- **GPT capture DRIFT — REVISI status (bukan drift):** `div[data-message-author-role="assistant"] .markdown` MASIH match 3 node (bukan 0). Failure capture lalu = **page salah** (`chrome://new-tab-page/`), bukan selector mati. `data-testid="copy-turn-action-button"` = 5 (terlalu lebar: `Salin pesan`/`Salin tabel`/composer) → jangan jadi anchor utama. `Salin respons` count = 3.
- **Z capture OK:** `z/bridge-cdp-z_new.ts` jalan bersih — pakai `.copy-response-button` + bubble `div[class*="message-"]`, `waitForFunction` DOM (bukan visibility) → hindari bug GPT. Paste `ai_role_answers.md` ke Z new chat (Profile 14) sukses, Z balas "initialized as AI Agents Answering node" (file itu persona def, bukan pertanyaan — behavior benar).
- **Staged lock-down gotcha:** kalau pakai key YANG SAMA, jangan taruh plain-line + scoped-line berdampingan — OpenSSH match baris PERTAMA, jadi plain (shell) menang. Dengan 1 key: langsung single scoped line. Staged aman HANYA kalau pakai key BEDA (test key).

## [2026-07-17] Gemini remote port (Profile 2) — transport + skill + command + import dirs
- **Gemini di-bridge via Profile 2** (BUKAN Profile 14). Transport: `gemini/bridge-cdp-gemini_new.ts` (homepage `https://gemini.google.com/app`) + `gemini/bridge-cdp-gemini_continue.ts` (`https://gemini.google.com/app/993698fe8a26cae6`). Keduanya default `BRIDGE_PROFILE='Profile 2'`. `_new.ts` punya `logConversation` → `web-bridge-gemini.log` (field `profile`, `transport: 'gemini/bridge-cdp-gemini_new.ts'`, `extractConvId` match `/app/<uuid>`). `_continue.ts` BELUM tulis `.log` (konsisten dgn gpt/claude/z continue).
- **FOKUS TRIK Gemini:** tekan `/` → sleep 0.5s → `Backspace` → sleep 0.5s = auto-fokus chat input (berbeda dari ChatGPT `Shift+Esc`, Claude `r`+Backspace, Z klik `#chat-input`). Lalu `Ctrl/Cmd+V` paste, `Enter` send.
- **STOP-button Gemini** (`web-dom-gemini §3.1`): deteksi "masih menjawab" = cek `[data-mat-icon-name="stop"]` / `[fonticon="stop"]` ADA → generate. Bukan copy-button (copy-button = capture only).
- **Scrape Gemini:** `message-content` node terakhir, innerText = AUTHORITATIVE, clipboard DITOLAK (KOTOR). Copy button best-effort = `button[aria-label="Copy"]` / `.copy-button`. Selector BEST-EFFORT (not live-verified) — re-verify snapshot sebelum aksi kritis.
- **Skill:** `.claude/skills/web-dom-gemini/SKILL.md` (confidence: not-live-observed); `web-dom-general` enumerasi `web-dom-gemini` di 4 tempat (L8, L12/L13, L198-199, L233).
- **Command:** `.claude/commands/webchain-gemini.md` (clone webchain-z, Profile 2, `GQI`/`GAI` dirs, dispatch verifier dgn dir Gemini).
- **Import dirs (terpisah dari gpt/claude/z):** `Geometry_Engine/gemini_questions_import/` + `gemini_answers_import/` (README + scaffold: `by_date/17-07-2026/`, `temp_questions_all.md`, `temp_questions_single.md` (purity), `log_questions_17-07-2026.md`, `temp_answers.md`, `temp_knowledges/`, `bank_knowledges/`).
- **Wiring:** `list_profil_vendor.md` §1 (Profile 2 + Gemini), §2 (URL↔profil Gemini), §3 (log), `AGENTS.md PHASE 0.5` pengecualian Gemini=Profile 2, `tsconfig.json` include 2 gemini transport. `tsc --noEmit` PASS.

## [2026-07-17] Kendala *_new.ts: capture URL sesi SEBELUM refresh (F5)
- **BUG:** refresh (F5) di homepage `*_new.ts` membuka chat BARU → sesi + jejak URL hilang → chain LOOP tanpa pernah dapat capture jawaban.
- **FIX (wajib, terapkan ke semua `*_new.ts` gpt/claude/z/gemini):** transport HARUS catat URL sesi ke `web-bridge-<remote>.log` **tepat 2 detik SETELAH pesan PERTAMA dikirim** (sebelum ada refresh). Vendor mutasi URL homepage → `/c|chat|app/<uuid>` beberapa detik setelah send; URL itu = satu-satunya jejak sesi. Bila perlu refresh, **RE-OPEN URL itu** sbg pengganti F5 (jangan F5 di homepage).
- **Implementasi:** helper `captureSessionUrl(page, {profile, promptChars})` → `sleep(SESSION_CAPTURE_DELAY_MS=2000)` → `logConversation(...)` (mode `send`, pageOwned:false). Delay override `BRIDGE_SESSION_CAPTURE_DELAY_MS`. Dipanggil di `sendAndWaitForReply` SETELAH send, SEBELUM `waitForStableReply`. `.log` baris punya `url`+`convId`.
- **Rule global:** tulis ke `web-dom-general §4.1` (capture URL sesi sebelum refresh). `tsc --noEmit` PASS.

## [2026-07-17 11:30] User address / protocol notes (MASTER)
- Panggil user **"MASTER"**.
- MASTER buka/tutup akses SSH secara manual (plain ↔ scoped) saat butuh. Saat scoped, agent TIDAK bisa tulis file bebas (termasuk `C:\Users\ryoro\.ssh\authorized_keys2`) — benar-benar read/gatekeeper-only.
- Git commit trailer = `CodeName: AVA` (tanpa `Co-Authored-By`/email). Port 20120–20130 = agent, jangan kill.

## [2026-07-17] Claude Web live bridge — kritis DOM drift + send fix (Profile 2)
- **Q15/Q16/Q17 dijawab LIVE di claude.ai (Profile 2), conversation `133920ad-a1d1-4582-a5e7-b968c41a299d`.** Jawaban tersimpan di `claude_answers_import/temp_answers.md` (Q15=3477, Q16=4048, Q17=4609 char).
- **BUG KRITIS 1 — tombol Send case-sensitive:** selector lama `button[aria-label="Send Message"]` (capital M) TIDAK match; live `aria-label="Send message"` (lowercase m). CSS attr selector case-SENSITIVE → `sendVisible` selalu null → fallback click gagal → draft Q15 menumpuk tak terkirim. FIX: pakai flag `i` → `button[aria-label="Send message" i], button[aria-label="Send" i], button[type="submit"]`.
- **BUG KRITIS 2 — reply selector MATI:** `div[data-testid="assistant-message"]` & `[data-message-author-role="assistant"]` SUDAH TIDAK ADA di Claude live 2026-07-17. Tiap pesan (user+assistant per turn) sekarang dibungkus **`div[role="article"]`**; balasan terakhir = elemen TERAKHIR. Copy button = **`button[data-testid="action-bar-copy"]`** (bukan `aria-label="Copy"`).
- **SEND otoritatif = KLIK tombol** (bukan Shift+Enter). Shift+Enter terbukti GAGAL submit di layout tertentu (Q15 stuck). Urutan: fokus trik `r`→Backspace → `Ctrl+V` paste → **klik tombol Send**.
- **URL sesi mutasi SETELAH kirim:** capture URL `/chat/<uuid>` HARUS setelah klik Send (sebelumnya di-cap sebelum kirim → selalu `claude.ai/new`). Pindah `captureSessionUrl` ke sesudah klik send di kedua transport.
- **Terapkan ke:** kedua transport `claude/bridge-cdp-claude_new.ts` + `_continue.ts` (selector `ASSISTANT_MSG='div[role="article"]'`, `COPY_BUTTON='button[data-testid="action-bar-copy"]'`, `SEND_BUTTON` case-insensitive, send=klik tombol). `web-dom-claude` §3 (scrape) + §Send button + shortcut table di-update; `metadata.confidence=live-observed`. `tsc --noEmit` PASS.
- **§8 Mid-task adaptation policy** DITAMBAH ke `web-dom-general` (MASTER 2026-07-17): saat confidence <50% di tengah task, izin fix/extend/adapt asal "know what you're doing" + BACKUP original ke `setup_docs/BAK/<vendor>/<file>.<timestamp>.bak`. Kedua claude transport di-backup ke `BAK/claude/*.20260717-033851.bak`.
