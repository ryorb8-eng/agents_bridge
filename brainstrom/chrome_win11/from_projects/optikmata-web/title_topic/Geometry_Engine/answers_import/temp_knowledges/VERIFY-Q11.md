# VERIFY Q11 — Flag-combo guard

- Q: `resolveGeometryEngineFlags` (types.ts:343-368) treats frame/lens/bridge/temple/anchor as independent booleans and does not prevent `frame_enabled=true` + `lens_enabled=false`, which triggers D-B overpaint. Q11a: should `frame_enabled` implicitly require `lens_enabled`, OR fix D-B so the lens fx-clone follows the aperture at split (no guard needed)?
- Answer seen: Recommends NOT making `frame_enabled` implicitly force `lens_enabled`. D-B is a render-pipeline correctness bug (lens FX uses full-lens geometry while base is aperture-only), not a config bug. Fix D-B first (lens FX follows `lensAperture`), then add an explicit flag validator + documented supported-combination matrix (Valid / Valid-but-unusual+warning / Unsupported+reject) rather than masking the bug with a fake dependency.
- Verdict: KEEP (≥70%)
- Confidence: 88%
- Evidence: Internally consistent with `questions_import/by_date/16-07-2026/00_NEED_IMPROVE_16JULI.md`: D-C (line 26) states "Tidak ada guard `frame_enabled` butuh `lens_enabled` → kombo risk D-B | major (arch) | types.ts:343-368"; Stuck points (line 105) list "Flag-combo risk (D-C): `frame` butuh `lens`? Guard di resolver?". D-B (line 25) describes "Lens fx-clone full-lens shape saat frame split → band ke-overpaint" (glassesLayout.tsx:455-524 vs :415-434) — matches the answer's diagnosis that the renderer uses the wrong source geometry (full lens instead of aperture). Q10 framing (`temp_questions_all.md` lines 128-134) confirms the D-B mechanism. The answer directly answers Q11a (both branches) and is appropriately hedged that frame-without-lens validity is a product/UX decision (architect-owned), recommending a validator instead of asserting. No contradicting claim against the diagnosis. (External ref: Martin Fowler "Feature Toggles" — unverifiable as a code fact but non-load-bearing design citation; not counted against confidence.)
- Claims to bank (if KEEP/PARTIAL):
  - D-B (lens fx-clone overpaint during frame split) is a render-pipeline correctness bug, not a flag-config bug; it should be fixed at the source (lens FX follows `lensApertureD`) rather than masked by a flag guard.
  - `resolveGeometryEngineFlags` treats frame/lens/bridge/temple/anchor as independent booleans with no guard preventing `frame_enabled=true` + `lens_enabled=false` (this is D-C).
  - `frame_enabled` should NOT implicitly force `lens_enabled`; frame-without-lens can be a valid product state (frame demo, empty rim, preview, editor, wireframe) depending on product spec — this is architect-owned.
  - Prefer an explicit flag validator + documented supported-combination matrix (Valid / Valid-but-unusual with optional warning / Unsupported rejected with clear message) over implicit flag dependencies; combo-validity decisions must come from the data model, not from a renderer bug.
  - A guard should only exist when a flag combo has no semantic meaning (e.g., bridge-enabled + frame-disabled; anchor-enabled + geometry-disabled), and even then the decision is architect-owned.
  - Fixing D-B first (lens FX follows aperture) makes `frame_enabled=true` + `lens_enabled=false` render correctly, so no guard is needed for correctness.
- Open issues / follow-up Q (if any): The recommendation is sound but assumes the product's intended supported-combination matrix, which is not defined in the diagnosis materials. A follow-up architect decision is needed: is frame-without-lens an intended supported state, and what is the explicit list of supported/unsupported combos for the validator? (This is UX-direction, architect-owned — out of scope for a coding agent.)
