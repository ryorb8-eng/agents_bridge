# VERIFY Q12 — Live VTO nose-bridge attach

- Q: Live /vto anchors frame to iris-centroid (only 2 iris centers read) so bridge/nosepad float vertically on atypical faces; NOSE_TOP/NOSE_BOTTOM exist for this but are dead (D-3). (a) Consume nasal landmark (mesh pt 6/168) as vertical bridge anchor? (b) `anchor` flag already TRUE in prod (a1e6e5e) but has no consumer — should enabling it require wiring NOSE_TOP/NOSE_BOTTOM in render?
- Answer seen: Q12a YES — use nasal landmark as an *additional vertical constraint* (not a replacement for iris); derive bridge geometry from NOSE_TOP/NOSE_BOTTOM via Anchor Engine, keeping iris for IPD/rotation/scale. Q12b — `anchor enabled` is not "complete" until producer→geometry→renderer→parity are all wired; flag-on-with-empty-consumer should be documented as partial rollout. Recommends semantic-anchor abstraction over reading raw Face Mesh indices in the renderer.
- Verdict: KEEP (≥70%)
- Confidence: 85%
- Evidence:
  - Premises match diagnosis: D-D fix in `questions_import/by_date/16-07-2026/02_LIVE_VTO_FACE.md:73-74` explicitly states "consume nasal landmark (mesh pt 6/168) utk vertical bridge anchor; flag anchor sdh TRUE tp consumer tdk ada" — answer's Q12a mirrors this verbatim.
  - Dead anchors confirmed: `00_NEED_IMPROVE_16JULI.md:28` (D-3: NOSE_TOP/BOTTOM mati) and `questions_import/temp_questions_all.md:49-53` (flag `geometry_engine_anchor_enabled` SUDAH TRUE di prod but nose tdk terbentuk). Answer's Q12b (flag-on/no-consumer = partial) is consistent with stuck-point #4 in `00_NEED_IMPROVE_16JULI.md:106`.
  - Architecture reasoning consistent with Anchor Decoupling lesson cited in Q3a (`temp_questions_all.md:56-57`): derive from invariant semantic anchors, not raw base nodes. Answer's "don't bind renderer to raw landmark indices" aligns.
  - Iris-only horizontal/IPD/rotation anchor acknowledged in source: `vto/page.tsx:485,493-498` and iris-centroid anchor `glassesLayout.tsx:808-815` (per D-D evidence `02_LIVE_VTO_FACE.md:64`). Answer correctly preserves iris for scale/orientation.
  - Honest self-limitation: answer states it CANNOT confirm 6/168 are the optimal landmark pair without seeing the actual Face Mesh model, and deliberately recommends semantic-anchor contract instead of hard-coding indices — this is correct epistemic behavior (ADR-0004: untrusted-peer reasoning, flagged uncertainty).
  - External references (MediaPipe Face Mesh docs, SVG2 coords) are general background, not code-factual claims about optikmata-web; not load-bearing.
- Claims to bank (if KEEP/PARTIAL):
  - Live VTO should keep iris-centroid for horizontal placement/scale/rotation (IPD, eye-line angle) but add a nasal-landmark *vertical* bridge constraint so the bridge follows the nose root, not the eye midpoint, fixing vertical float on atypical-proportion faces.
  - Bridge vertical geometry should be derived from the existing NOSE_TOP/NOSE_BOTTOM semantic anchors (produced by Anchor Engine) rather than the renderer reading raw Face Mesh landmark indices directly (decouples renderer from landmark model changes).
  - `geometry_engine_anchor_enabled = true` with no render consumer is a partial/empty rollout, not a complete feature; the flag should only be considered production-ready after producer→geometry→renderer are wired AND parity is validated.
  - Recommended pipeline: MediaPipe/Face Mesh → Anchor Engine (IRIS/NOSE/HINGE/Others) → Geometry Generator → Renderer, so renderer never depends on raw landmark indices.
  - Real-time use of NOSE_TOP/NOSE_BOTTOM for bridge placement needs temporal smoothing/filtering to avoid inter-frame jitter, and all semantic anchors must share one coordinate system before geometry generation.
- Open issues / follow-up Q (if any): The answer cannot confirm whether Face Mesh points 6/168 are the optimal nasal-landmark pair for this specific implementation — follow-up should verify against the actual landmark model in `app/vto/**` (and the exact anchor produced by AnchorEngine for NOSE_TOP/NOSE_BOTTOM). Also unconfirmed from repo materials: whether `a1e6e5e` is the real prod commit that set anchor=true (claim originates from the question text, not independently verified).
