# VERIFY Q13 — Live VTO roll/yaw/blink/oversize/dead-signal

- Q: Live `/vto` (D-E): roll/yaw unhandled (2D eye-line rotate only); face lost -> ipd=0 -> ModularGlassesSVG null -> glasses vanish then snap (vto/page.tsx:500-508); adjustScale:1.20 live default (ManualDesain.tsx:56) -> oversize ~20%; buyerFaceSignal (pillar-3 face-follow) collected but NOT wired to placement (dead-end). Sub-Q: (a) debounce/clamp on face-lost? (b) adjustScale:1.20 intentional vs bug? (c) wire buyerFaceSignal or delete it?
- Answer seen: (Q13a) Strongly recommends temporal tracking (hold + debounce + confidence gating state machine TRACKING->LOST->HOLD->TIMEOUT->HIDDEN) instead of returning null. (Q13b) Honestly states it CANNOT conclude 1.20 is a bug from the value alone; gives two possibilities (intentional calibration vs historical tuning) and recommends not keeping it hardcoded without validation, instead making it a configurable calibration/profile parameter. (Q13c) Recommends NOT deleting if pillar-3 roadmap is active (mark as deferred scaffold, wire when ready) and delete only if roadmap cancelled, to avoid a silent dead pipeline.
- Verdict: KEEP (≥70%)
- Confidence: 85%
- Evidence:
  - All factual premises ChatGPT accepts are straight from the diagnosis `questions_import/by_date/16-07-2026/02_LIVE_VTO_FACE.md` D-E (lines 44-57): blink-vanish pipeline (face lost -> ipd=0 -> ModularGlassesSVG.tsx:92 null -> vto/page.tsx:508 snap); adjustScale:1.20 (ManualDesain.tsx:56) oversize ~20%; dead buyerFaceSignal (vto/page.tsx:176-199,551,1138-1140 not wired to ModularGlassesSVG props). Internally consistent.
  - Q13a: recommendation (hold/debounce/confidence gating) directly resolves the D-E "no clamp" gap; the source itself lists "debounce/clamp saat face lost" as a fix (02_LIVE_VTO_FACE.md:75). Consistent, sound AR/VTO practice.
  - Q13b: ChatGPT refuses to assert bug-vs-intentional and the source independently flags the SAME thing as an OPEN QUESTION ("adjustScale:1.20 intentional margin atau over-scale bug?", 02_LIVE_VTO_FACE.md:83). So the honest-uncertainty stance is fully consistent with the diagnosis; no false certainty.
  - Q13c: conditional wire-or-delete mirrors the source fix "wire buyerFaceSignal ke placement (atau hapus spy tdk misleading)" (02_LIVE_VTO_FACE.md:76) and the open question re pillar-3 deferred/unimplemented (line 84). Consistent.
  - The answer adds NO new unverifiable code/file:line claims. The only external link is a MediaPipe Face Landmarker reference — a generic, well-known public doc, non-load-bearing to the conclusions; treat as unverifiable-but-low-risk (not required for the verdict).
  - The diagnosis notes roll/yaw are NOT handled (2D eye-line rotate only); ChatGPT's recommendations focus on face-lost/scale/signal and do not claim to fix roll/yaw — appropriate scope, matches Q13a/b/c which also do not ask to fix roll/yaw.
- Claims to bank (if KEEP/PARTIAL):
  - D-E: Live `/vto` face-lost -> ipd=0 -> ModularGlassesSVG returns null -> glasses vanish then snap back; there is NO hold/debounce/clamp, so tracking noise/occlusion/blink (1-5 frames) causes flicker and popping. (source: 02_LIVE_VTO_FACE.md:48-49, vto/page.tsx:508)
  - D-E: adjustScale:1.20 is the live default (ManualDesain.tsx:56) and scales glasses ~20% over true PD; whether this is intentional margin or over-scale bug is UNVERIFIED (open question, not resolvable from value alone). (source: 02_LIVE_VTO_FACE.md:50-51,83)
  - D-E: buyerFaceSignal (pillar-3 face-follow) is collected and passed to feedback/CTA (vto/page.tsx:176-199,551,1138-1140) but is NOT wired to ModularGlassesSVG placement props -> it is a dead-end producer with no consumer. (source: 02_LIVE_VTO_FACE.md:52-54)
  - Recommended D-E fix patterns: (a) add a tracking state machine with confidence gating + hold last pose + debounce + timeout fade (eliminates snap/flicker); (b) replace hardcoded adjustScale:1.20 with a measured-scale x calibration-scale parameter that is configurable/profile/device-scoped and documented; (c) give buyerFaceSignal explicit status — keep+mark deferred if pillar-3 roadmap active (wire on implementation) or delete if roadmap cancelled. (synthesized from ChatGPT answer + 02_LIVE_VTO_FACE.md:75-76)
- Open issues / follow-up Q (if any): Whether adjustScale:1.20 is intentional vs bug (Q13b) and whether buyerFaceSignal/pillar-3 is active or cancelled (Q13c) remain DECISIONS that need the Architect / design-history data to close — ChatGPT correctly refused to invent an answer. Roll/yaw/perspective (head-turn misalignment, no z-depth) were noted in the diagnosis but are out of scope of Q13a/b/c and still unaddressed.
