# VERIFY Q14 — GeometryEditor no-op controls

- Q: GeometryEditor (D-F) has hidden-by-default editor + Rotate/Mirror/Save/Compile/Clone controls that are silent no-ops. Q14a: thread handlers (handleSaveFrame/compile/clone) via partProps OR delete the buttons? Q14b: wire Rotate/Mirror to SVG transform OR delete until affine-matrix primitive ready (admin marks matrix ops "roadmap / NOT YET")?
- Answer seen: Conditional recommendation — never leave a silent no-op interactive UI. For Save/Compile/Clone: thread handlers via partProps if the pipeline is ready, otherwise disable the buttons with a "Not Yet Implemented" status. For Rotate/Mirror: do NOT partially wire to the renderer until the affine/matrix primitive is an official Geometry Engine foundation; instead mark as roadmap, disable UI, or show "Coming Soon", because a partial SVG transform without updating anchors/bounds/FX/compilation risks pipeline inconsistency (Mirror is harder: winding, path orientation, anchor, left/right semantics).
- Verdict: KEEP (≥70%)
- Confidence: 90%
- Evidence: Internally consistent with source diagnosis. D-F (`00_NEED_IMPROVE_16JULI.md:29,75-84` and `03_EDITOR_MATERIAL_RESPONSIVE.md:3-16`) confirms: editor hidden by default (dual-gate, `ManualDesain.tsx:194`); Rotate+Mirror persist to state but renderer never reads → silent no-op (`GeometryEditor.tsx:148-169`; header `:21-23` admits "renderer may ignore it"); Save/Compile/Clone render but no-op because `partProps` omits `onSave`/`onCompile`/`onClone` (`ManualDesain.tsx:171-179`; `GeometryEditor.tsx:189-217`). Source also notes admin marks anchor (matrix) controls "roadmap / NOT YET" (`03_EDITOR_MATERIAL_RESPONSIVE.md:14-16`; `geometry-engine/page.tsx:80-83`), matching ChatGPT's Q14b recommendation to defer wiring until the affine-matrix primitive is ready. The question itself frames Q14b identically ("hapus sampai affine-matrix primitive ready — admin mark matrix ops 'roadmap / NOT YET'"). ChatGPT answers both sub-questions, echos the source findings, and gives sound conditional guidance appropriate to an architect-owned decision. No contradiction found. Line ranges in source vary slightly (`:99-169` vs `:148-169`, `:21-23` header) but describe the same no-op regions; immaterial.
- Claims to bank (if KEEP/PARTIAL):
  - GeometryEditor's Save/Compile/Clone buttons are silent no-ops because `partProps` is built without `onSave`/`onCompile`/`onClone` handlers, which are invoked as `onSave?.()` etc. (undefined) in `GeometryEditor.tsx:189-217` (`ManualDesain.tsx:171-179`).
  - Rotate/Mirror persist to React state but the renderer (glassesLayout/ModularGlassesSVG/composite) never consumes them, so they are silent no-ops (`GeometryEditor.tsx:148-169`; `:21-23` admits "renderer may ignore it").
  - Architecture/UX principle: interactive UI controls must never be silent no-ops; if a handler's pipeline is not ready, the control should be disabled with a visible "Not Yet Implemented / Roadmap / Coming Soon" status rather than left clickable-but-inert (consistency with source "roadmap / NOT YET" admin marking for matrix ops).
  - Rotate/Mirror should not be wired to a partial SVG transform before an official affine/matrix primitive exists in Geometry Engine, because Anchor/Bounds/FX/Selection/Hit-test/Compilation would keep using the old geometry and become inconsistent; Mirror additionally affects winding, path orientation, anchors, and left/right semantics.
  - Recommended fix for D-F: thread the handlers via `partProps` when the pipeline is ready, otherwise disable or hide the controls.
- Open issues / follow-up Q (if any): The answer defers the final thread-vs-disable choice to "actual implementation readiness" (architect decision) — consistent with the question being flagged architect-owned. No factual gap to reject; follow-up could confirm whether `handleSaveFrame`/compile/clone handlers already exist and are wired to persistence, to decide thread-vs-disable. ChatGPT's cited references (Nielsen Norman heuristics, Martin Fowler feature toggles) are general UI/UX principles, not repo-specific file:line claims, so treated as accepted public knowledge rather than unverifiable repo claims.
