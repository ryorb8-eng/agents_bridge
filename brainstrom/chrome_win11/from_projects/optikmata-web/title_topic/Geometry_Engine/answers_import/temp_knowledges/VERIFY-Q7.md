# VERIFY Q7 — Temple flag no-op + parity violation

- Q: Q7a — Is the temple-toggle (`geometry_engine_temple_enabled`) intended as a no-op, and if so is the admin "LIVE" label (`geometry-engine/page.tsx:96`) misleading / should it change? Q7b — How to add a TRUE show/hide gate (prop `showTemple` / flag `temple_enabled` independent of the compiler) so the toggle has real effect WITHOUT breaking legacy-path parity ("default OFF ⇒ byte-identical legacy")?
- Answer seen: Confirms the current toggle is effectively a no-op (two generators yield identical geometry; temple always rendered unconditionally) and that the "LIVE"/"Enable Temple" label is misleading if it only swaps the compiler backend. For Q7b it proposes splitting Compiler Strategy (`templeGeometryStrategy` LEGACY/COMPILED, affects generator) from Render Visibility (`showTemple` true/false, affects renderer via `if (!showTemple) return;`), keeping legacy parity by default.
- Verdict: KEEP (≥70%)
- Confidence: 88%
- Evidence:
  - No-op premise matches diagnosis D-6/D-7: `geometry_engine_temple_enabled` only swaps direct-generator ↔ compiled-centerline, both call `generateTempleArmD()` → bit-identical geometry; toggle has no visual effect (`00_NEED_IMPROVE_16JULI.md` D-6 lines 20, 38-48; `01_TEMPLE_HINGE.md` D-6 lines 6-11).
  - "Always render" premise matches D-7: component `'temple'` pushed unconditionally even when all flags OFF, violating the "default OFF ⇒ byte-identik legacy" invariant (`:200-201`, `:794-806`) — `00_NEED_IMPROVE_16JULI.md` lines 21, 38-48; `01_TEMPLE_HINGE.md` lines 12-16, 50-54.
  - Misleading "LIVE" label claim matches diagnosis: admin label "LIVE" (page.tsx:96) misleading because nothing changes visually — `01_TEMPLE_HINGE.md` lines 9-10, 50-51.
  - Q7b recommendation matches the diagnosis's own ARCHITECT-owned fix verbatim: "add TRUE show/hide gate (prop `showTemple` / flag `temple_enabled` independent of compiler), not just source-swap" — `00_NEED_IMPROVE_16JULI.md` lines 47-48, 102-103; `01_TEMPLE_HINGE.md` lines 42-44, 52-53. The answer's two-flag split (strategy vs visibility) is a sound realization of that fix and explicitly preserves legacy parity.
  - Honesty check: answer correctly declines to assert intent ("cannot conclude without design docs") — consistent with the diagnosis framing the intent as an OPEN QUESTION (`01_TEMPLE_HINGE.md` lines 50-54), not a settled fact. This is appropriate, not a gap.
  - unverifiable: the answer cites external references (Martin Fowler Feature Toggles, Wikipedia SRP) as support. These are general software-engineering principles, not codebase facts; not verifiable from repo materials but low-risk and not in conflict with anything.
  - No contradiction found between the answer and the 16-07-2026 source materials. The answer does not introduce new file:line claims that conflict with the described defect facts.
- Claims to bank (if KEEP/PARTIAL):
  - The temple toggle (`geometry_engine_temple_enabled`) is currently a no-op in visual terms: it only swaps the temple generator backend, and both backends call `generateTempleArmD()` producing byte-identical geometry.
  - The temple component is pushed to the render unconditionally (even with all GE flags OFF), which violates the "default OFF ⇒ byte-identical legacy" parity invariant — temple is the only part that is never dormant.
  - If the flag only swaps the compiler backend, an admin label such as "LIVE" / "Enable Temple" is misleading; it should be relabeled (e.g. "Temple Geometry Backend" / "Temple Compiler") and the visual on/off control (e.g. "Show Temple") kept separate.
  - A correct fix separates two concerns: a geometry-strategy flag (LEGACY/COMPILED, affects only the generator) and an independent `showTemple` visibility flag (renderer gate `if (!showTemple) return;`). This gives the toggle real effect while defaulting to legacy behavior to preserve byte-identical parity.
  - Whether the no-op is intended design or an oversight is undetermined — requires design docs / architect decision; the diagnosis itself lists it as an open question, so it is ARCHITECT-owned.
- Open issues / follow-up Q (if any): Whether temple should ever be hidden by default (for normal glasses a temple is usually mandatory) is a product/UX decision the answer flags as needing clarification — this is architect territory and overlaps with D-9 (right-temple hinge symmetry at non-default bridge offset). If `showTemple=false` is ever used in production, the fixed right-temple hinge (mirror-NoseBridgeAnchorMiddle) behavior under `adjustNosebridgeX` offset should be re-validated.
