# VERIFY Q8 — Temple shading exclusion

- Q: (Q8a) Should the temple get its own fx-clone (extend `isFramePart` to `'temple'`) or be flat by-design (and if flat, delete the dead-clone compute)? (Q8b) Parity risk — temple flat in `flat` profile but shaded in `studio`: is that visual inconsistency acceptable?
- Answer seen: ChatGPT recommends temple become part of the same FX system as the frame but via a *different intensity profile* (own Temple FX Profile), not identical to lens/bridge; if temple is decided always-flat, delete the dead-clone compute. On Q8b it rejects flat-in-one-profile-but-shaded-in-another *when it stems from different render logic*; all profiles should share the same pipeline, with only FX intensity / layer-toggle differing per profile.
- Verdict: KEEP (≥70%)
- Confidence: 88%
- Evidence:
  - Core factual chain matches the diagnosis exactly: `isFramePart = lens || bridge` (`glassesLayout.tsx:931`, per question + `00_NEED_IMPROVE_16JULI.md:19` + `01_TEMPLE_HINGE.md:38`) → temple `isFramePart=false` → `applyFx=false` → 7 fx-clone blocks (outline/fill/ao/bevel/reflection/depth) skipped (`:932,938-979`, `01_TEMPLE_HINGE.md:38`) → temple clone `:753-759` computed-but-discarded = dead code (`00_NEED_IMPROVE_16JULI.md:21-24`, `01_TEMPLE_HINGE.md:38`). Consistent.
  - Q8a answer aligns with the two D-8 fix options offered in `00_NEED_IMPROVE_16JULI.md:45-46` ("extend `isFramePart` to `'temple'` (reuse the already-computed clone) OR flat by-design but delete dead-clone compute"). ChatGPT picks option 1 with the nuance of a *separate* (lower-intensity) profile rather than full rim profile — sound reasoning (thin/long arm vs thick/curved rim avoids over-shading).
  - Q8b answer correctly frames the parity rule: flat-in-`flat` vs shaded-in-`studio` is acceptable **only if** driven by per-profile FX intensity/layer-toggle over a shared pipeline, NOT by reclassifying temple as FramePart in one profile but not another (that changes the render graph and breaks parity/regression testing). Consistent with the "parity-invariant" concern in `00_NEED_IMPROVE_16JULI.md:12-16,102-103`.
  - unverifiable: all numeric FX-Profile parameters (temple Outline 0.4 / AO 0.15 / Reflection 0.25; the Rim/Bridge/Temple table Outline 0.4/AO 0.2/Reflection 0.2/Depth 0.2 for temple; per-profile examples Flat 0, Preview Outline 0.2/AO 0.15, Studio Outline 0.4/AO 0.3/Reflection 0.2, Premium Outline 0.5/AO 0.5/Reflection 0.4) are ChatGPT's own design proposals, NOT sourced from the repo — treat as calibration suggestions, not codebase facts. Also unverifiable: the claim that the `flat` profile's purpose is debugging/wireframe/editor (reasonable inference, not stated in 16-07 materials).
- Claims to bank (if KEEP/PARTIAL):
  - D-8 dead code: temple is excluded from all shading because `isFramePart = lens || bridge` (`glassesLayout.tsx:931`) makes temple `isFramePart=false` → `applyFx=false` → the 7 fx-clone blocks (outline/fill/ao/bevel/reflection/depth) are skipped, while the temple clone (`glassesLayout.tsx:753-759`) is still computed but discarded = dead render path.
  - Recommended fix (architect-owned, matches `00_NEED_IMPROVE_16JULI.md:45-46`): extend `isFramePart` to include `'temple'` and reuse the already-computed clone so temple runs through the same FX pipeline, but with its own lower-intensity Temple FX Profile rather than the full rim/bridge profile (avoids over-shading on a thin/long arm).
  - Alternative if temple is decided flat-by-design: delete the dead-clone compute (`:753-759`); do not keep code that is never consumed.
  - Parity rule for Q8b: temple flat in `flat` profile but shaded in `studio` is acceptable ONLY if it comes from different FX intensity / layer-toggle per profile over a shared pipeline — NOT if it comes from temple being classified as a FramePart in one profile but not another (that changes the render graph and breaks parity/regression tests).
- Open issues / follow-up Q (if any): The FX-Profile numeric parameters are ChatGPT's design proposals, not repo-sourced — they need real product calibration (Architect-owned). Whether a product decision genuinely wants temple always flat (e.g. a specific illustration style) remains undetermined without the full implementation; this is the D-8 open question 3 (`00_NEED_IMPROVE_16JULI.md:53`).
