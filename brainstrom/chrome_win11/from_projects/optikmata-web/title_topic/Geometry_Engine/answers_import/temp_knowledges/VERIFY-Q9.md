# VERIFY Q9 — CRITICAL: frame-split partType match fragility

- Q: (Q9a) Should `.find(p=>p.partType==='lens')` be replaced with an `id==='lens'` match (like aperture is matched by id)? (Q9b) Is a geometry-level fx-clone parity test (not just `lensBounds`) needed for frame-ON combos, since D-A/D-B gaps are invisible to the current test suite?
- Answer seen: Yes to both. Q9a: switch lookup from classifying `partType` to unique `id` (identity vs classification), with caveat that it only holds if the schema guarantees id uniqueness, plus suggestion of an id→part registry/constant. Q9b: `lensBounds` alone is insufficient (bounds can stay identical while the FX-clone source geometry flips), so a geometry parity test (part id, source path, clone source, visual regression) is needed, focused on Frame ON where outline/AO/depth/reflection are active.
- Verdict: KEEP (≥70%)
- Confidence: 90%
- Evidence: Fully consistent with the diagnosis in `questions_import/by_date/16-07-2026/00_NEED_IMPROVE_16JULI.md` — D-A section (lines 50-58, 104, 119) states `compiledLensPaths(compiled,'lens')` uses `.find(p=>p.partType==='lens')` at `glassesLayout.tsx:236-242,285-287`, recipe has 2 parts `partType:'lens'` (full + lens-aperture), correctness relies on merge order `ModularGlassesSVG.tsx:73` before `:77`, and it contradicts the "Do NOT match by partType" warning at `:293-294`. The answer's order-dependence / classification-vs-identity reasoning and the false-negative (bounds PASS but FX source wrong) risk match the Q9b premise exactly. The answer itself correctly hedges the only weak point: id-based match presupposes `id` is unique/stable in the schema (unverifiable from repo materials — target codebase optikmata-web is external/not in this repo; answer flags this and recommends schema validation). End references are SVG2 spec URLs (general standards), not repo-specific claims.
- Claims to bank (if KEEP/PARTIAL):
  - `compiledLensPaths(compiled,'lens')` uses `.find(p=>p.partType==='lens')` which is order-dependent and fragile because the recipe contains 2 parts with `partType:'lens'` (full lens + lens-aperture); correctness today depends solely on merge order (`ModularGlassesSVG.tsx:73` before `:77`) and contradicts the in-code "Do NOT match by partType" warning (`:293-294`).
  - Matching the full lens by `id==='lens'` (as aperture is already matched by id) is a more robust fix than matching by `partType`, but only if the schema guarantees id uniqueness; otherwise build an id→part registry/constant and add schema validation to catch duplicate ids.
  - `lensBounds`-only tests are insufficient: a merge-order flip can keep bounds identical (false negative / test PASS) while changing the FX-clone source geometry (`baseRawLensElement` becomes aperture-only), so a geometry-level fx-clone parity test (part identifier, source path d, clone source geometry, visual regression) is required, focused on Frame ON where outline/AO/depth/reflection paths are active.
  - Note on provenance: all file:line anchors above come from the 16-07-2026 diagnosis materials, not from the actual optikmata-web repo (external, not present in this workspace), so they are accepted-as-stated by the diagnosis rather than independently confirmed.
- Open issues / follow-up Q (if any): The schema-uniqueness of `id` is assumed but not verified — a follow-up could confirm whether `platformConfig/types.ts` (or the recipe schema) guarantees unique `id` before applying the Q9a fix; if not, the registry/validation approach is the safe path.
